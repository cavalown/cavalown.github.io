---
import CollectionIndex from "@components/CollectionIndex.astro";
import { getEntries } from "@data/content";

const locale = "en";
const collection = "studio";
const entries = await getEntries(collection);
---

<CollectionIndex locale={locale} collection={collection} entries={entries} />
